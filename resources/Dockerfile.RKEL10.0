# Start from the base image you pulled
FROM rockylinux/rockylinux:10.0

# Set a working directory inside the container
WORKDIR /opt/stalwart

# Copy the install script into the container
COPY install.sh .

# Run the installation script (ensure necessary build tools are installed first)
RUN dnf update -y && \
    dnf install -y curl git make gcc && \
    sh install.sh && \
    dnf clean all

COPY resources/libfdb_c.so /lib64/
RUN ldconfig

# Expose the required ports
EXPOSE 443
EXPOSE 25
EXPOSE 110
EXPOSE 587
EXPOSE 465
EXPOSE 143
EXPOSE 993
EXPOSE 995
EXPOSE 4190
EXPOSE 8080

# You can also list them all on one line, separated by spaces:
# EXPOSE 443 25 110 587 465 143 993 995 4190 8080

# If your "stalwa" application runs as a service or a command,
# specify the default command to run when the container starts.
# CMD ["stalwa", "start-service"] # Example command、
Docker 镜像打包与上传
构建并测试完镜像后，您可以选择以下两种方式进行打包和上传：

方式一：推送到 Docker 镜像仓库 (推荐)
这是分享和部署 Docker 镜像的标准方法。最常用的是 Docker Hub。
为镜像打标签 (Tag)：
将您的本地镜像标记为符合镜像仓库规范的名称。例如，推送到 Docker Hub：

Bash

docker tag my-stalwa-image:latest your_dockerhub_username/stalwa-app:1.0
请将 your_dockerhub_username 替换为您的 Docker Hub 用户名。

登录 Docker Hub：
如果尚未登录，需要先登录到 Docker Hub：

Bash

docker login
输入您的用户名和密码。

推送镜像：
将打好标签的镜像推送到仓库：

Bash

docker push your_dockerhub_username/stalwa-app:1.0
方式二：保存为 Tar 归档文件 (本地传输或备份)
如果您需要离线传输镜像，或者只是想做一个本地备份，可以将镜像保存为 .tar 文件。

保存镜像：

Bash

docker save -o stalwa-app.tar my-stalwa-image:latest
这会在当前目录下创建一个名为 stalwa-app.tar 的文件。

加载镜像 (在其他机器上)：
将 stalwa-app.tar 文件传输到目标机器后，使用以下命令加载：

Bash

docker load -i stalwa-app.tar

