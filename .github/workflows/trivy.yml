name: Trivy CI - Multi-Arch

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '00 12 * * *'

permissions:
  contents: read

jobs:
  trivy-scan:
    permissions:
      contents: read
      security-events: write
      actions: read
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: [linux/amd64, linux/arm64]
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      # ----------------------------
      # Build Docker image (multi-arch)
      # ----------------------------
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build multi-arch Docker image
        run: |
          docker buildx build \
            --platform ${{ matrix.platform }} \
            --tag ghcr.io/offsoc/stalwart:test-scan-${{ matrix.platform//\//- }} \
            --load \
            .

      # ----------------------------
      # Scan filesystem
      # ----------------------------
      - name: Scan filesystem with Trivy
        uses: aquasecurity/trivy-action@v0.42.1
        with:
          scan-type: fs
          ignore-unfixed: true
          format: sarif
          output: trivy-fs-results-${{ matrix.platform//\//- }}.sarif
          severity: CRITICAL,HIGH
          exit-code: 1

      - name: Upload filesystem SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-fs-results-${{ matrix.platform//\//- }}.sarif

      # ----------------------------
      # Scan Docker image
      # ----------------------------
      - name: Scan Docker image with Trivy
        uses: aquasecurity/trivy-action@v0.42.1
        with:
          scan-type: image
          image-ref: ghcr.io/offsoc/stalwart:test-scan-${{ matrix.platform//\//- }}
          format: sarif
          output: trivy-image-results-${{ matrix.platform//\//- }}.sarif
          severity: CRITICAL,HIGH
          exit-code: 1

      - name: Upload Docker image SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-image-results-${{ matrix.platform//\//- }}.sarif
